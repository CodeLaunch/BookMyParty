<?xml version="1.0" encoding="utf-8" ?>
<target name="war" description="Create a war file">
  	<!-- strip out test components from delivered war (and vice versa if building test client) -->
      <zip destfile="${project.name}.war" basedir="war" />
  </target>